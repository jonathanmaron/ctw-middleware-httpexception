<?php
declare(strict_types=1);

use Ctw\Http\Entity;
use Ctw\Http\HttpException;

/** @var Entity\HttpStatus $entity */
$entity = $this->entity;

/** @var HttpException\NotFoundException $exception */
$exception = $this->exception;

echo serialize([$entity, $exception]);
